import 'package:flutter/material.dart';
import 'components/body.dart';

class GetStart extends StatelessWidget {
  static String id = 'GetStart';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
